apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  namespace: llm-proxy
  labels:
    app: llm-proxy
spec:
  selector:
    app: llm-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
